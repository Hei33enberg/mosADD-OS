/**
 * @m0ssad/bridges — multi-platform reach for mosadd
 *
 * Each bridge exposes a new m* module (mMATRIX, mDISCORD, mTELEGRAM,
 * mSLACK, mSIGNAL, mWHATSAPP, mIMESSAGE) so mosadd users can reach
 * contacts who haven't switched yet.
 *
 * Structurally derived from Nous Research's Hermes Agent
 * gateway/platforms/ (MIT). See NOTICE.
 */

export const VERSION = "3.0.0-alpha.0" as const;
