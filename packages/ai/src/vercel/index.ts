/**
 * @m0ssad/ai/vercel — Vercel AI SDK adapter
 *
 * Status: stub. Real implementation lands in v3.0.0-alpha.1.
 */

export function mosadd(_options: { apiKey?: string } = {}) {
  // TODO: return a Vercel AI SDK `tools` object whose calls round-trip
  // through @m0ssad/mcp.
  return {};
}
