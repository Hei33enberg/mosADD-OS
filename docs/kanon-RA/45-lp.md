# LP — strona marketingowa mosADD

Rozdział o stronie, nie o aplikacji. Wygląd aplikacji jest w 10-mosadd. Kod: `m0ssad-3/apps/web/src/components/landing/`.

## Nagłówek — decyzja Króla 19.09 08:35

„na LP ma być w headerze wyłącznie piramida Volt, która otwiera menu, a nie jakieś ikony na zewnątrz … i CTA. Ma być jak było wcześniej … ostatni raz ci mówię” (DECYZJE-KROLA.md, wpis GOD OF RA DESIGN, 19.09).

Stan kodu zgodny (`LandingHeader.tsx`):
- lewa strona: znak słowny, `Wordmark size={16} fluid tm` (`:84`);
- prawa strona: zielone CTA + piramida mVAULT jako jedyna kontrolka (`:88`, `:98`, `:109` — `PyramidIcon` ze słońcem `hsl(var(--primary))`), klik otwiera `LandingVaultPanel` (`:114`);
- ⛔ pasek czterech sygnetów NIE wchodzi na LP. Sygnety są kanonem nagłówka APLIKACJI (10-mosadd), nie strony.
- Cel dotyku 44 px: piramida zostaje 36 px, łapie niewidzialny `::after` 36+8 (`:46`) — ta sama reguła co w apce, nigdy przez margines.
- Nagłówek jest przezroczysty nad hero (`:9-11`).
- Gdy panel VAULT jest otwarty, rysuje własny znak i własne zamknięcie piramidą (`:63`) — nagłówek się nie dubluje.

## Hero

`RadarHero.tsx`: brew, jedna linia nagłówka, podnagłówek, CTA i prawdziwe linki pobrania per platforma, czarne płótno radaru.

⛔ Zero licznika w hero. Do 05.09 stał tam `HeroLiveUsers` z trzema licznikami, które nie czytały niczego z serwera — liczby brały się z `base * (0.5 + Math.random()*1.4)`, więc ten sam gość przy trzech odświeżeniach widział 5 629 / 7 575 / 8 693 „ludzi” (zmierzone @375 px). Rozkaz: prawdziwa liczba ALBO brak sekcji. Pilnuje tego `src/components/landing/__tests__/heroNoFabricatedCounts.test.tsx`. Przywrócenie: najpierw endpoint z prawdziwą liczbą, potem UI.

## Stopka — decyzja Króla 19.09 07:48

Układ jak stopka Cursora: grupy ZAWSZE otwarte, bez harmonijki i strzałek, szary tytuł grupy, pod nim same linki. Na telefonie dwie kolumny (Product · Resources, Company · Legal, Connect), dalej `md:grid-cols-3 lg:grid-cols-6` (`LPFooter.tsx:242`). „Legal” stoi jako czwarta grupa, jak u Cursora (`:162`). LP-A i LP-B tak samo. Dolny pasek: wersja + motto (`:289-294`).

⛔ Historia, żeby nie wróciła: nazwy grup „Product / Builders / Company / Legal” były nieprawdziwe (`:7`), a dwie z sześciu kolumn miały nierówne szerokości 198/304/234 (`:88`). Nazwa grupy opisuje, co w niej jest, albo grupy nie ma.

## Wersje strony

| Adres | Co to | Źródło |
|---|---|---|
| `/` | LP A, baza 14.09 | DECYZJE 18.09 |
| `/b` | LP B — podgląd, `noindex`. Podmiana `/` na LP B wyłącznie po TAK Króla | DECYZJE 18.09 |
| `/c` | to samo LP-B, „wrzuć normalnie na mosadd.com/c” | Król 19.09 03:17 |

LP LAB: wiele wersji strony głównej na testy konwersji („nawet 33”) pod buying personę. Mechanizm — losowanie ludzi na `/`, boty zawsze kontrola, pomiar bez PII, bandyta dopiero w fazie 2 — to ustalenie ADMIRAŁA i GOD OF RA DESIGN, ⏳ NIE decyzja Króla: `C:\Users\Public\LP-LAB-33-MECHANIZM-19-09.md`.

## Reguły tekstu na LP

- H1 w IBM Plex Mono 700 WERSALIKAMI (`mosadd-lp-h1`), H2 w Space Grotesk (`mosadd-lp-h2`). ⚠ R4 uznaje Space Grotesk za wycofany, a H2 jeszcze go noszą.
- Minimum tekstu HTML: 11 px (zamówienie LP-029).
- Ton: krótko, do właściciela firmy, wojskowo. Nagłówek `1-MAN-ARMY OPS DECK`. Rejestr i słownik uczciwości — 60-glos.
- CTA: `[ BUILD YOUR ARMY — FREE ]` → `/auth`, zawsze ten sam cel; ≤600 px wolno skrócić do `[ START FREE ]`.

## Ruch na LP

Osobny rozdział: 70-ruch. Tu tylko zasada: wizual marki na LP gra także przy systemowym Reduce Motion (decyzja Króla 11.08), reszta aplikacji reduce-motion honoruje.
