{
  "name": "@mosadd/channel0-ext",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "channel 0 [mIRC] — with mosadd inside. Anonymous live chat scoped to the domain you're on.",
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --config vite.config.content.ts && node scripts/finalize.mjs",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "zip": "cd dist && zip -r ../channel0-ext.zip ."
  },
  "dependencies": {
    "@mosadd/skins": "file:../../packages/skins"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
